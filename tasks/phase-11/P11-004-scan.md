# P11-004: Path Normalization Consumer Scan

## Result: 20 call sites across 8 files

| File | Line | Pattern | Replacement Safe? |
|------|------|---------|-------------------|
| config/mod.rs | 1276 | `dunce::canonicalize(p).ok()` | ⚠️ path may not exist |
| config/mod.rs | 1608 | `dunce::canonicalize(candidate)` | ✅ path is loaded file |
| config/mod.rs | 1620 | `dunce::canonicalize(&base)?` | ✅ base should exist |
| config/mod.rs | 1627 | `dunce::canonicalize(&grok_home)` | ⚠️ fallback if missing |
| config/watcher.rs | 170 | `dunce::canonicalize(&h)` | ✅ home dir exists |
| config/watcher.rs | 339 | `dunce::canonicalize(parent)` | ✅ parent exists |
| config/watcher.rs | 450 | `dunce::canonicalize(p)` | ⚠️ may not exist |
| extensions/skills.rs | 171 | `dunce::canonicalize(&absolute)` | ✅ file just verified |
| extensions/skills.rs | 587 | `dunce::canonicalize(&sub)` | ⚠️ test code |
| extensions/skills.rs | 600 | `dunce::canonicalize(tmp.path())` | ✅ test temp dir |
| leader/mod.rs | 1386 | `dunce::canonicalize(path)` | ⚠️ fallback |
| session/fs_watch.rs | 42 | `dunce::canonicalize` | ✅ both exist |
| session/telemetry.rs | 66 | `dunce::canonicalize` | ⚠️ fallback |
| session/worktree_pool.rs | 1911+ | `dunce::canonicalize` | ✅ test code |

**Blocking issue:** `normalized_absolute()` returns `Err(PathError::NotFound)` for non-existent paths, while `dunce::canonicalize` succeeds. ~40% of call sites would regress.

**Next:** Either update `normalized_absolute` to fallback like `dunce` (Phase 3 change), or fix each call site individually per-task.

## Status
⏳ 分析完成，等待决定处理方案。
