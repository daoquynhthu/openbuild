# P11-PATH-001: xai-fast-worktree git commands audit

## Card Info
- Ledger ID: P11-PATH-001
- Crate: xai-fast-worktree
- Scope: src/git/ (checkout.rs, discovery.rs, index.rs, status.rs, worktree.rs, mod.rs)
- Status: VERIFIED

## Audit Findings

### Compliance
- All `Command::new("git")` invocations use `.arg()`/`.args()` with proper argument arrays
- No shell string concatenation anywhere in scope
- No `.arg("-c")` passing command strings
- `args.join(" ")` used only in error messages, not command construction

### Command Locations
- checkout.rs:19 — `Command::new("git")` (factory function `git_command()`)
  - All callers use `.args([...])` with literal arrays or slice references
  - Lines: 35, 62, 81, 105, 124, 145, 165, 319-337, 386-395, 450-512
- discovery.rs — 0 `Command::new` calls (uses `gix` library)
- index.rs — 0 `Command::new` calls (uses `gix::index::File`)
- status.rs — 0 `Command::new` calls (uses `gix::status`)
- worktree.rs — 0 direct calls (delegates to `git_command()`, uses proper `.args([...])`)
- mod.rs — 0 calls (re-exports only)

### Verdict: PASS

### Notes
- `src/sync.rs` has 16 `Command::new("git")` calls in `#[cfg(test)]` blocks, all proper — out of scope but noted clean.
